export namespace DbTypes {
	export type CharacteristicsResult = {
		name: string,
		title: string
	}

	export type TotalCharacteristics = {
		name: string,
		count: number
	}

	export type PhotoResult = {
		name: string
	}
}
